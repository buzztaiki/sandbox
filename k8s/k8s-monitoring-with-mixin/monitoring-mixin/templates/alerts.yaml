{{/* -*-mode: k8s-helm-*- */}}
{{- $currentScope := . }}
{{- range $path, $_ :=  .Files.Glob  "generated/*/alerts.yaml" }}
{{- with $currentScope }}
{{- $name := dir $path | base | printf "%s-alert" }}
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "monitoring-mixin.fullname" . }}-{{ $name }}
  annotations:
    {{- with .Values.alert.annotations }}{{- toYaml . | nindent 4 }}{{- end }}
  labels:
    {{- include "monitoring-mixin.labels" . | nindent 4 }}
    {{- with .Values.alert.labels }}{{- toYaml . | nindent 4 }}{{- end }}
spec:
  {{- .Files.Get $path | nindent 2 }}
{{- end }}
{{- end }}
