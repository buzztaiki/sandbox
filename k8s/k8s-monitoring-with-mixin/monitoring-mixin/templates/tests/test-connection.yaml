apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monitoring-mixin.fullname" . }}-test-connection"
  labels:
    {{- include "monitoring-mixin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monitoring-mixin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
