apiVersion: v1
kind: Service
metadata:
  name: {{ include "monitoring-mixin.fullname" . }}
  labels:
    {{- include "monitoring-mixin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monitoring-mixin.selectorLabels" . | nindent 4 }}
